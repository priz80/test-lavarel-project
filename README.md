# Test Laravel Project

Простой API для управления задачами (CRUD) на Laravel 12.  
Проект демонстрирует работу с SQLite, валидацией данных и RESTful-маршрутами.

---

## 🚀 Функционал

- Создание, просмотр, обновление и удаление задач
- Хранение данных в **SQLite** (без необходимости в MySQL/PostgreSQL)
- Полная **валидация входных данных**
- REST API по стандарту JSON

---

## 🛠️ Технологии

- [Laravel 12](https://laravel.com)
- PHP 8.3+
- SQLite (легковесная встроенная БД)
- Composer
- Git

---

## 🔽 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/ваше-имя/test-laravel-project.git
cd test-laravel-project

---

### 2. Установите зависимости

```bash
composer install
```

---

### 3. Настройте окружение
Создайте файл .env на основе примера:

```bash
cp .env.example .env
```

#### Убедитесь, что в .env указано:

DB_CONNECTION=sqlite

---

### 4. Создайте базу данных SQLite

```bash
touch database/database.sqlite
```

### Файл будет автоматически использоваться как БД.

---

### 5. Выполните миграции

```bash
php artisan migrate
```

### Это создаст таблицу tasks

---

### 6. Запустите сервер

```bash
php artisan serve
```

### API доступно по адресу: http://localhost:8000/api/tasks

## 📥 API Endpoints

| Метод | Эндпоинт | Описание |
|--------|------------|------------------------|
| GET | `/api/tasks` | Список всех задач |
| POST | `/api/tasks` | Создать новую задачу |
| GET | `/api/tasks/{id}` | Просмотр одной задачи |
| PUT | `/api/tasks/{id}` | Обновить задачу |
| DELETE | `/api/tasks/{id}` | Удалить задачу |

## ✅ Валидация данных

При создании и обновлении задачи применяется строгая валидация:

| Поле | Правила | Описание |
|-------|-----------------------------------------------|----------------------------------|
| title | обязательное, строка, мин. 1 символ, макс. 255 | Не может быть пустым |
| description | опциональное, строка, макс. 1000 символов | Может отсутствовать |
| status | обязательное, одно из: pending, in_progress, completed | Защита от неверных значений |

 Пример ошибки валидации:

{
  "message": "The given data was invalid.",
  "errors": {
    "title": ["Поле \"title\" обязательно для заполнения."]
  }
}

## 💡 Примеры запросов

 Создать задачу

```http
POST http://localhost:8000/api/tasks
Content-Type: application/json

{
  "title": "Написать документацию",
  "description": "Создать README.md",
  "status": "in_progress"
}
```

 Получить все задачи

```http
GET http://localhost:8000/api/tasks
```

## 🗂️ Структура проекта (ключевые файлы)

| Файл | Назначение |
|------|-----------|
| `app/Models/Task.php` | Модель задачи |
| `app/Http/Controllers/TaskController.php` | Логика API |
| `routes/api.php` | Маршруты |
| `database/migrations/*_create_tasks_table.php` | Структура БД |
| `.env` | Конфигурация (не загружается в репозиторий) |
| `.env.example` | Шаблон для настройки |

## 🧹 Очистка кэша (при необходимости)

```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
```

## 📄 Лицензия

MIT

---
